<h4 class="text-xl sm:text-xl lg:text-3xl leading-none font-extrabold text-gray-900 tracking-tight mb-8">
  BackMakers
</h4>

<% if @backmakers.empty? %>
<p>There's no one here yet 😱</p>
<% end %>

<ul class="backmakers">
<% @backmakers.each do |bm| %>
<li><%= bm.name %></li>
<% end %>
</ol>

<%= link_to 'Add new BackMaker', 
              action: 'new' %>

